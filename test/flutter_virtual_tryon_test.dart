import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder until the public API lands in M1 (doc/API.md). This keeps
  // `flutter test` green through M0 rather than leaving an empty test/.
  test('package scaffold is importable', () {
    expect(true, isTrue);
  });
}
